extends index

block content
  .container
    form.form-signin(role='form', action="/doAddSlice",method="post", style='max-width: 300px;')
      input.form-control(type='text', name="title", placeholder='Slice Name')
      button.btn.btn-lg.btn-primary.btn-block(type='submit') Add Slice
